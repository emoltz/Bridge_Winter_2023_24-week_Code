#include <iostream>
#include <cmath>
using namespace std;


int main() {


    cout.setf(ios::fixed);
    cout.setf(ios::showpoint);
    //the number in the parens below can set the precision of the decimal points.
    cout.precision(1);


    return 0;
}